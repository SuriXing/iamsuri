# F3.10 MyRoom — Architect

## Code-Quality Audit
- data/rooms.ts frozen: 🔵 — `git diff dc24245~1 dc24245 -- src/world3d/data/rooms.ts` is empty. Commit touches only `rooms/MyRoom.tsx`, `parts/Bookshelf.tsx`, `parts/DeskLamp.tsx`. Room metadata untouched.
- flatShading: 🔵 — Every `meshPhongMaterial` in `MyRoom.tsx` (lines 88, 94, 100, 106, 111, 124, 131, 143, 151, 157, 163, 170, 176, 182, 188, 198, 203, 208, 213, 219, 223, 229, 233, 270, 276, 280, 284, 288, 295, 301, 308, 313, 318), `Bookshelf.tsx` (124, 132, 144, 152), and `DeskLamp.tsx` (71, 76, 81) carries the `flatShading` prop. Zero misses across ~40 materials.
- useFrame allocs: 🔵 — Only `DeskLamp.tsx:50-64` subscribes to `useFrame`. It reads `clock.getElapsedTime()`, computes `Math.sin(t * 1.6) * PULSE_AMPLITUDE`, and mutates `mat.emissiveIntensity` + `light.intensity` via refs. No `new THREE.X()`, no object/array literals, no closures captured per frame. Hot path is scalar math only.
- Backward-compat parts: 🔵 — `Bookshelf` props `seed?`, `heroBookCount = 0`, `edgeColor = '#0a0a14'` are all optional with defaults. The `rng === null` branch at `Bookshelf.tsx:102-104` preserves the legacy modulo pattern (`colorIdx = (row * 3 + b) % bookColors.length`, `h = 0.22 + ((row * 5 + b) % 5) * 0.018`). With no `seed` and no `heroBookCount`, output is bit-identical to the previous signature. `DeskLamp` `pulse` defaults to `false`, so the `useFrame` early-returns on non-pulsing instances — old callers unaffected.

## Material Variation (15)
🔵 13/15 — Palette spans pink family (4 shades: `PINK`, `PINK_SOFT`, `PINK_DARK`, `PINK_DUSTY`), wood (2 shades + `SHELF_BACK` + `FRAME_DARK`), whites (2 shades), gold accent, monitor dark-navy with pink emissive, terracotta pot, leaf green. Deterministic pillow tint pool pulls from 4 candidates via `makeRng(PILLOW_SEED)`. Emissive is used with intent: monitor screen (1.2), scanline (2.0), lamp head (1.8 pulsing), rug border (0.25), plant leaves, sheet fold, pillows, curtains — all subtle except the monitor which is the hero glow source. Loses 2: non-wood surfaces cluster tightly in the pink bucket, so the "variation" is really "many pinks"; true cross-family contrast is carried almost entirely by gold + shelf books.

## Edge / Silhouette Pop (15)
🔵 13/15 — `<Edges color={EDGE_COLOR} lineWidth={1 | 1.2}>` on bed base, mattress, sheet fold (1.2), both pillows, headboard, rug, desk top (1.2), drawer face (1.2), picture frame, and hero books via `heroBookCount={3}` + `edgeColor={EDGE_COLOR}` (Bookshelf `cell.isHero &&` at line 145). `EDGE_COLOR = '#0a0a14'` is near-black and reads hard against pink/white/wood. Two omissions stand out: monitor frame + screen, and both curtain panels — these are the largest flat rectangles in the room, so the absence of outlines there is visible against everything else. Bumping those adds silhouette cohesion.

## Proportions (15)
🟡 11/15 — Bed slab `1.5×0.18` + mattress `1.4×0.28` + headboard `1.5×0.7×0.12` reads correctly chibi. Desk top `1.4×0.08×1.6` with two-chunk tapered legs (`0.09²` top, `0.06²` bottom) — clever trick at zero cost. Drawer face `1.3×0.22×0.5`. Monitor is `1.0×0.65` on a `1.4m` desk — reads as a laptop screen rather than a proper monitor at this chibi scale; it should occupy ~0.75× desk width. Shelf `1.2×0.3` at `baseY 0.45` with `rowSpacing 0.5` puts the top plank near `y=1.45`, leaving the picture frame at `y=1.7` to share the same visual strip — will look crowded against the shelf top in the current view. Rug `1.8×1.2` centered at `bedZ + 0.7` extends from `bedZ + 0.1` to `bedZ + 1.3`; desk is at `deskZ - 0.7` to `deskZ + 0.8` (i.e. `bedZ - 1.0` to `bedZ + 0.5` since deskZ = bedZ). Rug z-range overlaps desk x-separated space — won't clip since they're on opposite x sides, but visually the rug sits mostly in front of the bed foot, which is fine.

## Micro-animations (15)
🟡 9/15 — Exactly one animated element: `DeskLamp` pulsing at `sin(t * 1.6) * 0.05` on head emissive + point-light intensity. Implementation is textbook-clean (refs + module scalars, zero alloc). But one subtle pulse in a room with a monitor, scanline, plant, curtains, and pink accent light is thin for a 15-point category. The scanline at `deskZ - 0.45` is a static bar, not an animated sweep — obvious missed win. Pink `pointLight` at `bedX, 0.9` is static. Curtains are rigid. Biggest scoring gap in the whole eval.

## Color Harmony (10)
🔵 9/10 — Pink-dominant palette with wood grounding and gold accent continuity (drawer handle, pen, curtain rod all share `GOLD = '#c9a14a'`). The `EDGE_COLOR = '#0a0a14'` is dark enough to unify without fighting the warm tones. Monitor pink emissive (`#ffb6c1`) + lamp warm (`#ffd6a8`) + pink accent light (`#f4a8b8`, `intensity 0.3`, `distance 3`) layer into a consistent warm glow. Plant green `#4ade80` is the only cool spike and it's scoped tight enough to read as intentional accent. Tight, coherent.

## Clutter/Props (10)
🔵 9/10 — Prop density is now strong: 3-box folded clothes stack, notebook + pen, potted plant (pot + leaves), gold drawer handle, monitor + stand + base + scanline, dual pillows, sheet fold, framed wall picture, curtains (rod + 2 panels), bordered rug, hero-outlined books. Room no longer reads as empty scaffolding. Still missing "lived-in" signals: no floor object (slippers/bag), no cup/mug on the desk, nothing on the headboard. Those are F3.11+ territory.

## Refinement (10)
🟡 7/10 — Positive refinement: rug inner border as 4 thin strips (not a texture), two-chunk tapered legs, drawer trim board at `y = 0.72`, drawer handle as a small sphere, pillow rotation offsets `±0.05 rad`, clothes stack rotated `0.12 rad` for asymmetry, deterministic pillow tint seeding. Concerns: (a) rug top is at `y = 0.075 + 0.03/2 = 0.090` and border strips are at `y = 0.091` — only `0.001` offset, z-fighting risk on mobile GPUs. (b) Curtain panels at `z = oz - 1.93` with `0.04` depth extend to `z = -1.95`; depending on wall thickness at `oz - 2.0` this may clip the back wall. (c) Notebook pink and pen gold/pink read similar from distance. These are small but exactly the kind of detail the refinement category is supposed to catch.

## Performance (10)
🔵 10/10 — All primitive geometries (`boxGeometry`, `cylinderGeometry`, small `sphereGeometry` at `(8,6)` and `(6,6)`). Mesh count in MyRoom ~55 — fine. One `useFrame` subscriber total, doing O(1) scalar math. `pillowTints` is `useMemo(..., [])` (stable). `Bookshelf` `cells` memo has a correct dep array. Two dynamic point lights in the room (`DeskLamp` + pink accent), both distance-capped (6 and 3 respectively). Zero per-frame alloc, zero work in React reconciliation after mount.

## Total: 81/100
## Verdict: 🟡 ITERATE (below 85 threshold)

## Top 3 fixes for F3.11
1. **Three more micro-animations, all reusing the DeskLamp pattern** (biggest leverage — micro-anim at 9/15 → target 13/15). (a) Monitor scanline sweep: `meshRef.position.y = baseScanlineY + Math.sin(t * 2.0) * 0.22` — sweeps across the screen face. (b) Plant leaf breathing: `leafRef.scale.setScalar(1 + Math.sin(t * 0.8) * 0.02)` — scale mutation is alloc-free. (c) Pink accent `pointLight` breathing: `lightRef.intensity = 0.3 * (1 + Math.sin(t * 0.6) * 0.1)`. All three are refs + scalar math, zero allocation, and can live inside existing part components or a new `<PulsingLight>` util that mirrors DeskLamp's structure.
2. **Fix two refinement bugs** (refinement 7 → 9): (a) Raise rug inner-border strips from `y = 0.091` to `y = 0.095` to eliminate z-fighting. (b) Pull curtain panels forward to `z = oz - 1.88` (or verify back-wall thickness and correct accordingly) to ensure they don't clip the wall. Zero visual cost, fixes real bugs.
3. **Put `<Edges>` on monitor frame + screen + both curtain panels, and bump monitor size to `1.2 × 0.72`** (edge pop 13 → 14, proportions 11 → 13). The monitor is the hero desk object and it currently reads too small + too soft. Also nudge the wall picture from `y = 1.7` to `y = 1.85` to decrowd the shelf top.
