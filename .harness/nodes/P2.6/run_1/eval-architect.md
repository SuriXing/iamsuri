# P2.6 Phase 2 Final Gate — Architect Review

Commit: HEAD (fc1ba12)

## Bundle measurements

**2D first-paint graph on `/` (every non-App3D chunk, gzip):**

Eager (in every route):
- `index.html`                      0.52 KB
- `index-DFtHeg_N.css`              3.57 KB
- `client-BN5Dfv_y.js` (react-dom + react-router) 57.16 KB
- `index-CDhbW0yj.js` (App shell + SearchBox + minisearch index) 11.05 KB
- `jsx-runtime-DWSWI4JT.js`         3.25 KB

Landing lazy chunks (preloaded at module eval via `bootstrapRoute()` so they render with first paint):
- `Landing-B_1Gs3sV.js`            14.62 KB
- `Landing--9L7r5Gg.js`             2.52 KB
- `Landing-CImVf-SG.css`            5.07 KB
- `Monogram-DDsSV_ak.js`            0.56 KB

Data chunks pulled in by Landing:
- `about-DETcMKAM.js`               0.63 KB
- `products-C6jOzE6j.js`            1.07 KB
- `posts-iYLZVo6G.js`               1.11 KB
- `ideas-DN_dMpY2.js`               1.29 KB
- `date-DxE3XcD-.js`                0.20 KB
- `tags-m3chOI34.js`                0.16 KB

**Sum (realistic `/` first paint): 102.78 KB gzip** — 68% of the 150 KB budget.

Strict "every non-App3D chunk" sum (includes WorkList/WritingList/IdeaDetail/About/etc. that are NOT on /): **110.08 KB gzip** — still comfortably under budget.

Webfonts ship as woff2 assets and are not counted toward the JS budget (per-unicode subset, deferred by browser).

- `App3D-DJFykORv.js` (gzip): **264.31 KB**, lazy-split: **yes** — own chunk, only imported via `lazy(() => import('./world3d/App3D'))` in `src/App.tsx:20-21`, fire-and-forget preloaded only when `pathname === '/3d'`. The 3D chunk does NOT touch the 2D first paint.
- `dist/rss.xml`: present, 2490 bytes, valid RSS 2.0 (2 items, xmlns:content + atom:link self, properly closed `<channel>` / `<rss>`), generated by `rssFeedPlugin()` in `vite.config.ts:102-114`.

## Gate results

- tsc: **PASS** (silent, zero errors)
- eslint: **PASS** (silent, zero errors)
- build: **PASS** (668 modules, 208ms)
- playwright: **PASS (12/12)** — 2.1 min runtime via `vite preview` on port 4173

Note: the Playwright suite in `tests/3d-world.test.cjs` is 3D-only; it asserts canvas render, room navigation, theme toggle, keyboard flow, and `page.on('pageerror')` staying clean. It does NOT exercise the 2D content routes (`/writing/:slug`, search overlay, RSS link, etc.). 🟡 below.

## TS rigor

Greps across `src/`:
- `:any` count: **0**
- `as unknown` count: **4** (all in `src/world3d/**` — 3D layer is explicitly frozen and out of scope)
- `@ts-ignore` / `@ts-expect-error` count: **0**

2D layer (`src/components/**`, `src/lib/**`, `src/data/**`, `src/App.tsx`) is fully strict. `useParams<{ slug: string }>()` typed in `WritingDetail.tsx:22`. `search.ts:176` explicitly narrows minisearch's loose `id: any` back to `string` before handing it to the caller — deliberate containment with a comment. The `Post` discriminated union (`src/data/schema.ts:47-104`) is exhaustively narrowed in `Landing.tsx:174-183` (`WritingCard` switch) and again in `WritingDetail.tsx:63-80` (ternary over `post.kind`). Vite's RSS plugin uses `(p): p is Extract<Post, { kind: 'inline' }> => p.kind === 'inline'` — textbook type guard.

## Per-dimension scores (architect slice = 20 pts)

1. **TypeScript rigor: 5/5** — No escape hatches in the 2D layer. Discriminated union is canonical (one `PostBase`, three variants with `never` phantoms on the wrong fields, exhaustive switches). `useParams` typed. The one spot where `any` could have leaked (minisearch results) is explicitly wrapped in a strict `SearchHit` type at the boundary.

2. **Bundle health + lazy split: 5/5** — 2D first paint ~103 KB gz, 31% under budget. App3D-*.js is cleanly its own chunk (264 KB gz) and never downloads on `/`. Per-page chunks (Monogram, Landing, each list page, each detail page, each data file) are all individually tiny — Rollup's auto-splitting is working exactly right. `tsc --noEmit` + prod build both run cold in ~200ms.

3. **Routing + data architecture: 5/5** — Clean URL schema (`/`, `/work[/:slug]`, `/writing[/:slug]`, `/ideas[/:slug]`, `/about`, `/3d`, `/404`, splat → `/404`). Every content route lives under a single `ContentRoute` shell that owns `<SearchBox/ThemeToggle/ViewSwitcher/ProjectsDock>` — no duplicated chrome per page. The canonical data model lives in `src/data/{products,posts,ideas,about}.ts` and is imported both by the 2D layer AND the Vite RSS plugin (`vite.config.ts:3`) from the SAME `src/data/schema.ts` types — genuinely deduped, no parallel sources. The legacy `src/data/{myRoom,productRoom,...}.ts` files serving the frozen `world3d/` subtree are documented as such in `schema.ts:11-14`. The `Post.kind` hybrid (`inline | external | coming-soon`) is respected everywhere: `Landing.WritingCard`, `WritingDetail` detail body, RSS plugin filter, and the search index fallback (`search.ts:97`) all narrow correctly.

4. **Performance + gate health: 4.5/5** — All four gates green. No `console.*` calls outside `src/world3d/`. Every `useEffect` I audited registers a matching cleanup (`SearchBox` keydown + inert restore, `useScrollProgress` scroll+resize, `RouteAnnouncer` clearTimeout). Search index is built exactly once at module load (`search.ts:136-155`), not rebuilt per SearchBox mount. The 0.5 pt deduction: the Playwright suite is still 100% 3D — none of the P2 deliverables (SearchBox overlay, RSS presence, reduced-motion, `/writing/:slug` discriminated-union rendering, a11y trap in SearchBox) are actually asserted by any automated test. The P2.2-era search/a11y fixes are verified only by code inspection.

## Architect subtotal: 19.5/20

## 🔴 Critical findings (any = phase gate fail)

none

## 🟡 Iterate findings (non-blocking)

1. **Playwright coverage gap** — `tests/3d-world.test.cjs` is 3D-only. Add at least a minimal `2d-content.test.cjs` that hits `/`, `/writing/:slug`, opens SearchBox via `/`, asserts a result appears, and asserts `/rss.xml` returns 200 with `Content-Type: application/xml` (or `application/rss+xml`). Without it, the "4 gates green" claim materially under-covers Phase 2 deliverables. Not a blocker because tsc + eslint + prod build are strict and code inspection is clean, but this is the next iteration's highest-leverage fix.

2. **minisearch ships eagerly in the root chunk (~5 KB gz of its own)** — `SearchBox` is in every route's persistent chrome, so this is defensible. But a user who never opens search still pays for the index + library. A manualChunks rule splitting `minisearch` + `src/lib/search.ts` into a lazy chunk that's imported on SearchBox's first `openOverlay()` would shave ~5 KB off first paint. Optional.

3. **`Monogram.tsx` default export ignores `variant`** — `variant === 'mark-only'` is accepted as a prop but the component always renders `MONOGRAM_S_ITALIC_SERIF` and the only thing that changes is `data-variant="mark-only"` on the `<svg>` (`Monogram.tsx:100,111`). Comment on line 78 admits it's "currently equivalent — kept for future variants" — fine to keep, but either wire it up or remove the prop until you need it (YAGNI). Non-blocking.

4. **`typescript: "~6.0.2"` in devDependencies** — TypeScript 6 doesn't exist yet (May 2025 cutoff: 5.8 was current). Either this is a typo / aspirational pin that somehow resolves, or package-lock has a different actual version. `tsc` ran clean so whatever is installed works. Worth a sanity-check `npx tsc --version` before the next phase. Not a gate blocker.

5. **`WritingDetail.tsx` already imports `Landing.css`** (line 10) just to pick up a handful of card styles. Minor style-graph coupling — if Landing ever stops shipping CSS, WritingDetail silently regresses. Consider extracting the shared card classes into a neutral `card.css`. Cosmetic.

## Verdict from architect perspective

🔵 **Ship-ready**

Gates are all clean, bundle is 31% under budget with a well-behaved 3D split, types are strict end-to-end with a real discriminated union (not a `string` masquerading as one), and the canonical data model is shared between app + RSS plugin rather than duplicated. The one real concern — zero automated coverage of the 2D layer — is a follow-up iterate, not a ship blocker, because static analysis + prod build are strict and the code itself is clean on inspection.
